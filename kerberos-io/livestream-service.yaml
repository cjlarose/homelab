apiVersion: v1
kind: Service
metadata:
  name: kerberos-io-livestream-service
  namespace: media
spec:
  ports:
  - port: 8889
    targetPort: 8889
    protocol: TCP
    name: http
  selector:
    app: kerberos-io
