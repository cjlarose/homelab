apiVersion: v1
kind: Service
metadata:
  name: lancache-dns-udp-load-balancer-service
  namespace: media
spec:
  type: LoadBalancer
  ports:
  - name: dns-udp
    port: 5353
    targetPort: 53
    protocol: UDP
  selector:
    app: lancache-dns
