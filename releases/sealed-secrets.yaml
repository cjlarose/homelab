apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: sealed-secrets
  namespace: media
spec:
  releaseName: sealed-secrets
  chart:
    repository: https://charts.helm.sh/stable
    name: sealed-secrets
    version: 1.8.0
