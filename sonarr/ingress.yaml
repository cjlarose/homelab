apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: sonarr-ingress
  namespace: media
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: sonarr.192.168.50.143.nip.io
    http:
      paths:
      - path: /
        backend:
          serviceName: sonarr-service
          servicePort: 80
