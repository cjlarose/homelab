apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: transmission-ingress
  namespace: media
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: transmission.192.168.50.143.nip.io
    http:
      paths:
      - path: /
        backend:
          serviceName: transmission-service
          servicePort: 80
